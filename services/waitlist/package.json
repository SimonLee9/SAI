{
  "name": "sai-waitlist",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --env production",
    "tail": "wrangler tail --env production",
    "kv:list": "wrangler kv key list --binding WAITLIST_KV --env production --remote",
    "kv:export": "wrangler kv key list --binding WAITLIST_KV --env production --remote > waitlist-keys.json"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.5.4",
    "wrangler": "^3.85.0"
  }
}
